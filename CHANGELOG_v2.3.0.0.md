# Stealth Ops v2.3.0.0

대상: `index.html`, `sw.js` (`CACHE_NAME` → **`stealth-ops-v10`**)

---

## 1. 원형 패드 위치 상향

`bottom: 26px` → **`112px`**. 폰을 잡은 엄지 위치에 맞췄습니다. 버튼과의 겹침은 없습니다(패드 x 18~168, 가장 가까운 상호작용 버튼 좌측 끝 184px).

## 2. 이동 중에도 달리기 버튼이 눌리도록

원인은 **중복 토글**이었습니다.

```js
sb.addEventListener('touchstart', toggle);
sb.addEventListener('click', e=>{ if(e.detail!==0) toggle(e); });
```

`touchstart`로 켜지고, 뒤따라오는 합성 `click`이 다시 꺼서 결과적으로 아무 일도 일어나지 않는 경우가 있었습니다. 특히 다른 손가락이 패드를 잡고 있을 때 이벤트 순서가 달라지면서 자주 발생합니다.

**수정**: 250ms 이내 중복 이벤트를 무시하는 시간 기반 가드. 웅크리기 버튼도 같은 구조라 함께 적용했고, 두 버튼 모두 짧은 진동 피드백을 추가했습니다.

## 3. 연막탄 / 기절탄 각 10개 제한

- 레벨당 각 **10개**
- 버튼에 잔량이 숫자로 표시되고, 0이 되면 흐려지며 조준 자체가 안 됩니다
- **체크포인트 재시작** → 잔량 유지
- **레벨 재시작** → 각 10개로 보충

## 4. 미션 3개 → 5개

기존 3개(선 연결 / 카드키 다이얼 / CCTV 코드)에 성격이 다른 2개를 추가했습니다.

### 금고 락픽 (서재, 770/230)
바늘이 좌우로 왕복합니다. **초록 구간에서 멈추면** 핀이 하나 걸리고, 3개를 모두 걸면 열립니다. 빗나가면 걸린 핀이 하나 풀립니다.

성공할수록 어려워집니다 — 구간 폭이 `30% → 23% → 16%`로 좁아지고 바늘 속도는 `0.75 → 1.03 → 1.31`로 빨라집니다. 구간 위치도 매번 무작위입니다. **반응 속도**를 요구하는 유일한 미션입니다.

### 배전반 복구 (파티오, 1300/1400)
4색 패널이 순서대로 점멸합니다. 같은 순서로 눌러야 하며, 3라운드에 걸쳐 길이가 `3 → 4 → 5`로 늘어납니다. 틀리면 그 라운드를 처음부터 다시 봅니다.

각 패널에 고유 음(440 / 600 / 760 / 920Hz)이 배정되어 있어 **소리로도 외울 수 있습니다.** 기존 미션이 전부 시각 기반이라 감각을 달리했습니다.

## 5. 소음기 제거 — 실탄 총성

"타앙" 소리를 4개 층으로 구성했습니다.

| 층 | 내용 |
|---|---|
| 크랙 | 5200Hz + 2400Hz 노이즈, 0.018초 — 공기를 찢는 순간 |
| 몸통 | 78Hz sine + 124Hz triangle + 300Hz 저역 노이즈 |
| 잔향 | 60ms / 150ms 뒤 저역 노이즈 2회 — 벽에 부딪혀 돌아오는 소리 |
| 슬라이드 | 95ms 뒤 1900Hz + 고역 노이즈 |

### 게임플레이가 크게 바뀝니다

총성 소음 반경 **720px**을 추가했습니다(참고: 동전 260, 달리기 250). 이제 **총을 쏘면 거의 확실히 비상벨이 울립니다.**

기존에는 소음기 저격이 안전한 제거 수단이었지만, 이제는 "최후의 수단"이 됩니다. 근접 처치와 유인의 가치가 상대적으로 올라갑니다.

## 6. 비상 증원 — 평상시 30명 → 비상시 130명

### 동작

```
비상벨 발동 → 0.8초 후부터 증원 시작
           → 0.55초마다 6명씩 투입
           → 약 10초 만에 100명 도달 (총 130명)
비상벨 해제 → 초당 33명씩 순차 철수 → 30명 복귀
```

- 등장 지점은 **플레이어로부터 460px 밖**의 벽·가구가 없는 무작위 위치 (코앞에서 튀어나오지 않음)
- 등장 시 주황 파티클 + 0.45초 페이드 인
- 목격 지점 주변으로 흩어져 수색하며, 알람이 유지되는 동안 계속 재조준
- 철수는 **플레이어가 안 보는 인원부터** 조용히 (눈앞에서 사라지지 않음)
- HUD에 `⚠ 비상벨 (32s) · 지원 87명` 표시

### 알람 해제 규칙 — 여기서 문제를 하나 발견했습니다

처음에는 "발각된 동안은 타이머가 멈춤"으로 만들었는데, 검증에서 **알람이 영원히 안 풀렸습니다.** 100명이 맵을 훑고 있으니 항상 누군가는 플레이어를 보고 있어서입니다. 철수 테스트가 계속 실패해서 발견했습니다.

**수정**: 타이머는 항상 흐르고, 새로 발각되면 `raiseAlarm()`이 45초로 다시 채웁니다. 결과적으로 규칙이 **"45초 동안 들키지 않으면 해제"**로 명확해졌습니다. 숨을 곳(가구 아래, 옷장, 연막)을 찾아 버티는 것이 유일한 해법이 됩니다.

증원 병력은 급히 투입된 설정이라 **시야 ×0.72, 청력 ×0.8**로 낮췄습니다. 이게 없으면 사실상 클리어가 불가능했습니다.

---

## 검증

```
=== 미션 5개 ===
  PASS  선 연결, 카드키 단말기, CCTV 해킹, 금고 락픽, 배전반 복구
  PASS  미션 지점 접근 가능
=== 장비 잔량 ===
  PASS  체크포인트 재시작: 잔량 유지 (3/0)
  PASS  레벨 재시작: 10개로 보충
=== 비상 증원 ===
    1초 : 36명 / 10초: 130명 / 20초: 130명
  PASS  플레이어 코앞 스폰 0건, 벽에 낀 증원 0명
=== 45초 은신 → 해제 ===
  PASS  알람 해제 확인
  PASS  증원 전원 철수 → 상주 30명 복귀
  PASS  재발동 정상
=== 안정성 ===
  PASS  증원 100명 상태 1000프레임 무크래시
```

### 성능 (130명)

```
updateEnemy x130 : 0.245 ms
시야 폴리곤      : 0.190 ms
합계 0.44 ms = 60fps 예산의 3%
```

**로직은 문제없습니다.** 130명이어도 30명일 때와 비슷한데, 증원 대부분이 `investigate` 상태라 시야 판정을 건너뛰기 때문입니다.

---

## 실기기 확인 — 이번엔 특히 중요합니다

### 렌더 성능 ← 최대 리스크

**스프라이트가 30개에서 130개로 4.3배가 됩니다.** 각 스프라이트는 그라디언트 셰이딩·림라이트·그림자를 그리므로 로직보다 훨씬 비쌉니다. 이건 Node 스텁에서 측정할 수 없습니다.

**비상벨을 일부러 울려서 FPS를 확인해주세요.** 심하게 떨어지면 `REINF_MAX`(100)를 낮추거나, 증원 스프라이트를 단순화하는 방향으로 가면 됩니다.

### 밸런스

- **총성이 비상벨을 부릅니다.** 총 한 발 = 130명이라 사격이 사실상 자살 행위가 될 수 있습니다. 과하면 `NOISE.gunshot`(720)을 낮추세요.
- 45초 은신이 현실적으로 가능한지 — 숨을 곳이 부족하면 은신처를 더 늘려야 합니다.
- 락픽 난이도 (구간 16%까지 좁아짐)
- 배전반 5개 순서 암기가 과하지 않은지

### 그 외

- 패드 위치 112px가 적절한지
- 달리기 버튼이 이제 확실히 눌리는지
- 총성이 "타앙"으로 들리는지

---

## 이전 버전 미확인 항목

팔 뻗기 가시성 · 원형 패드 데드존 · 백그라운드 소리 차단 · 가구 아래 실루엣 · 가구 111개 상태의 FPS

## 남은 작업

튜토리얼 · 애널리틱스 · 목표 방향 안내 · 탈출 페이즈 · 비살상 결박 · 장비 픽업 · 일일 챌린지 · 영어 지원 · 경로탐색 A* · 충돌 공간 분할 · 스프라이트 렌더 최적화
