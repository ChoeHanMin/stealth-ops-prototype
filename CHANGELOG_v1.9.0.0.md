# Stealth Ops v1.9.0.0 — 버그 수정 5종

대상: `index.html`, `sw.js` (`CACHE_NAME` → **`stealth-ops-v6`**)

---

## 1. 취소 버튼이 연막탄을 가리는 문제 — **v1.8.0.0에서 제가 만든 버그입니다**

버튼을 방사형으로 재배치하면서 `#aimCancelBtn`의 고정 좌표(`right:8px; bottom:225px`)를 그대로 뒀습니다.

```
연막탄   x 260~312, y 641~693
취소버튼 x 294~404, y 650~690   → x 294~312 구간 겹침
```

취소 버튼이 `z-index:45`라 위에 깔려서 연막탄이 안 눌렸습니다.

**수정**: 고정 좌표를 버리고 `layoutButtons()` 체계에 편입. 방사형 클러스터 바깥(반경 252px, 118°)에 배치하고 왼손 모드도 자동 반전됩니다.

### 함께 발견한 배치 문제 2건

기하 검증을 돌리면서 추가로 나왔습니다.

| 문제 | 원인 | 수정 |
|---|---|---|
| 사격 × 웅크리기 겹침 | v1.8에서 사격 버튼을 62→68px로 키우면서 간격 부족 | 안쪽 호 반경 98→112, 각도 재배분 |
| 십자키 × 사격/유인 겹침 | 412px 세로 화면에서 십자키 174px가 너무 넓음 | 십자키 150px로 축소 (키 56→48px) |

최종 검증(412×915): 버튼 간 최소 여유 **15px**, 화면 밖 0개, 십자키 영역 침범 **없음**.

---

## 2. 사격음과 발소리가 구분 안 되는 문제

```js
// 수정 전 — 둘 다 짧은 highpass 노이즈
발소리: playNoise(0.045, 0.045, 2600, 'highpass')
사격음: playNoise(0.05,  0.10,  3200, 'highpass')
```

길이도 필터 종류도 거의 같고 주파수만 600Hz 차이였습니다. 구분될 수 없는 설정이었습니다.

**수정**: 사격음을 완전히 다른 음색으로 재설계.

```js
playNoise(0.06, 0.40, 380, 'lowpass')   // 둔탁한 저역 '툭'
playTone(150, 0.10, 0.26, 'triangle')   // 저역 몸통
→ 55ms 후 playTone(2100, ...) + 고역 노이즈  // 슬라이드 왕복 금속음
```

발소리는 고역 스침, 사격은 저역 타격 + 금속음이라 음역대가 겹치지 않습니다. 발소리 음량도 0.045→0.034로 낮췄습니다.

---

## 3. 사격 시 팔 뻗기 애니메이션

팔과 총이 `fillRect` 고정값이라 발사해도 시각적 변화가 없었습니다.

**수정**: `player.firePose`(뻗은 자세, 1.6/s 감쇠) + `player.recoil`(반동, 7/s 감쇠) 도입.

- 발사 시 팔이 앞으로 **7px 뻗고** 총 길이도 3px 늘어남
- 반동으로 5px 밀렸다가 빠르게 복귀 (반동이 먼저 사라지고 자세는 천천히 풀림)
- `firePose > 0.15`면 반대쪽 **받침 팔**이 나타남 (양손 사격 자세)
- 총구 화염 (반동 크기에 비례한 타원, 0.25 이상일 때만)
- 화면 흔들림 4 추가

---

## 4. 비상벨이 울려도 적이 반응하지 않는 문제 — **가장 심각했습니다**

`alarmRaised`를 읽는 곳이 전체 코드에서 **BGM 음량과 HUD 텍스트, 단 두 곳**이었습니다. 적 AI에는 한 줄도 연결되어 있지 않았습니다.

거기에 전파 함수에도 두 겹의 제한이 있었습니다.

```js
if(d > 620) continue;              // 620px 밖은 무시
if(en.state==='idle') continue;    // 앉아있는 15명은 완전 제외
```

즉 **정지 근무자 15명은 어떤 상황에서도 영원히 반응하지 않았고**, 순찰병도 620px 안에 있을 때만 반응했습니다. 사실상 알람 시스템이 존재하지 않는 상태였습니다.

### 새 동작 (선택하신 방식)

`raiseAlarm(x, y, reason)` 신설. **거리 제한 없음.**

| 대상 | 반응 |
|---|---|
| 순찰 인원 (5명) | 맵 어디에 있든 **전원** 목격 지점으로. 45~175px 흩어져 10~16초 수색 |
| 정지 근무자 (15명) | 자리를 지키되 **일어나 경계 태세**(50초). 소리 난 방향으로 시선 전환 |

검증: 최원거리 **2,079px** 순찰병까지 반응 확인 (기존 620px 제한 초과), 앉은 인원 15/15 경계 전환 + 전원 자리 유지.

### 알람 발동 조건 (5곳 전부 연결)

시체 발견 / 동료 실종 / 침입자 목격 / 총성 감지 / 무전 점호 응답 없음

### 알람 중 경비 강화

- 이동속도 ×1.3
- 시야 거리 ×1.15
- HUD에 `⚠ 비상벨 (남은초)` 표시
- 비상벨 소리 추가 (880↔660Hz 3회 반복)

### 해제 후에도 경계 유지

45초 후 해제되지만(수색 중인 적이 있으면 10초씩 연장) 곧장 평상시로 돌아가지 않고 **전원 25초 경계 단계**를 거칩니다. `경보 해제 — 경비가 경계 태세를 유지합니다` 메시지가 뜹니다.

검증: 해제 직후 `{caution:5, idle:15}` → 최종 `{patrol:5, idle:15}` 정상 복귀.

---

## 5. 사거리가 짧고 레이저와 어긋나는 문제

```
레이저: 원점 = 총구(몸중심+24px), 사거리 430  → 최대 454px
총알:   원점 = 몸 중심,           사거리 420  → 최대 420px
```

원점도 사거리도 달라서 **34px 어긋났습니다.** 녹색 선 끝의 적은 구조적으로 맞힐 수 없었습니다.

**수정**:
- `FIRE_RANGE` 420 → **800**
- `muzzle()` 헬퍼 신설 — 레이저·총알·명중판정이 **모두 같은 원점** 사용
- 레이저도 `FIRE_RANGE`를 그대로 사용 (별도 상수 제거)

검증: 최대 도달 **804px**, 레이저 끝 좌표와 총알 끝 좌표 **완전 일치**, 600px 거리 적 사살 성공.

> **밸런스 주의**: 사거리가 1.9배가 됐습니다. 화면 세로가 약 900px이니 거의 화면 끝까지 닿습니다. 저격이 지나치게 강해지면 `FIRE_RANGE`만 낮추면 되고, 레이저는 자동으로 따라옵니다.

---

## 검증 결과

```
--- BUG5 사거리 ---
  PASS  FIRE_RANGE = 800
  PASS  최대 도달 804px
  PASS  레이저 끝 = 총알 끝 (완전 일치)
  PASS  600px 적 사살
--- BUG4 비상벨 ---
  PASS  순찰병 전원 반응 (최원거리 2079px)
  PASS  앉은 인원 15명 전원 경계
  PASS  앉은 인원은 자리 유지
--- BUG3 발사 애니메이션 ---
  PASS  발사 직후 뻗음 0.97 / 반동 0.88
  PASS  반동 먼저 회복, 자세는 천천히
--- BUG1 배치 ---
  PASS  버튼 간 최소 여유 15px, 겹침 0건
  PASS  십자키 영역 침범 없음
--- 안정성 ---
  PASS  2400프레임 랜덤조작 무크래시
```

**실기기 확인 필요** (스텁으로 검증 불가)

- 사격음이 발소리와 실제로 구분되는지 (스피커/이어폰 양쪽)
- 팔 뻗기 애니메이션이 12px 캐릭터에서 보이는지 — **안 보이면 `ext` 값을 7에서 더 키워야 합니다**
- 비상벨 난이도 — 순찰병 5명 전원이 몰려오면 상당히 어려울 수 있습니다
- 사거리 800의 밸런스
- 축소된 십자키(150px)가 손에 맞는지

---

## 남은 작업

**다음 배치**: 튜토리얼 · 애널리틱스 · 목표 방향 안내 · 탈출 페이즈 · 비살상 결박 · 시체 운반 페널티 · 장비 픽업 · 일일 챌린지 · 영어 지원

**구조 변경 (별도 파일)**: 아이소메트릭 전환(`iso_poc.html` 확인 후) · 경로탐색 A* · 단일 파일 분할
