# Stealth Ops v1.6.0.0 — Batch 1 적용 내역

작업일: 2026-07-28 / 대상: `index.html`, `sw.js`

배포 시 주의: `sw.js`의 `CACHE_NAME`을 `stealth-ops-v2` → **`stealth-ops-v3`** 로 이미 올려놨음.
GitHub에 `index.html` + `sw.js` 두 개만 교체하면 되고, Play Console 재제출은 불필요.

---

## 적용한 기능 (11종)

| # | 기능 | 내용 |
|---|---|---|
| 1 | 경계도 링 | 적 머리 위에 `detect` 0→100 원형 게이지. 노랑→주황→빨강 |
| 2 | 소음 시스템 통합 | `NOISE` 상수로 일원화 (웅크림 38 / 걷기 145 / 달리기 250 / 비명 170). 앉아있는 적도 소리 방향으로 고개를 돌림 |
| 3 | 달리기 + 스태미나 | `달리기` 버튼 추가. 속도 1.7배, 스태미나 30/s 소모·20/s 회복, HUD에 파란 바 |
| 6 | 알람 3단계 | `search` → `caution`(28초, 시야 1.2배·이동 1.2배) → 평상시. 알람 해제도 자동 |
| 7 | 목격 정보 공유 | `broadcastAlert()` — 반경 620px 동료가 마지막 목격 지점 주변으로 흩어져 수색 |
| 9 | 버디 전체 확대 + 무전 점호 | 순찰 인원 전원 2인 1조. 60초마다 점호, 인원 미확인 시 전원 경계 |
| 10 | 적 시야 표시 토글 | 설정 메뉴에 "적 시야 표시 (쉬움)" 추가. 상태별 색상(백/주황/적) |
| 14 | 랜덤화 | 다이얼 3자리·키패드 4자리 코드를 매 판 랜덤 생성 + 쪽지 텍스트 자동 동기화. 순찰 시작 지점도 셔플 |
| 16 | 킬캠 슬로우모션 | 근접 처치 0.55초 / 저격 0.4초 동안 시간 32% 속도 |
| 17 | 발각 스팅어 | 발각 순간 0.42초 정지 + 붉은 비네트 + 2음 스팅어 + 적 위에 큰 `!` |
| 19 | 챌린지 3종 | 유령(무발각) / 불살(무살상) / 전격전(5분) — 별점이 이 3종 달성 개수로 산출, localStorage에 개별 저장 |

---

## 함께 고친 기존 버그 (중요)

**정지 근무자 15명이 `search` 상태를 거치면 게임이 멈추는 크래시**

`updateEnemy()`의 patrol 분기가 `goal = en.path[en.wp]` 직후 `goal.x`에 바로 접근하는데,
정지 근무자는 `path`가 빈 배열이라 `goal`이 `undefined` → TypeError.
게임 루프 안에서 throw가 나면 `requestAnimationFrame(loop)`에 도달하지 못해 **화면이 완전히 얼어붙음.**

재현 경로가 실제로 존재했음:
- 정지 근무자가 플레이어를 발각 → `search` → 타이머 만료 → `state='patrol'` → 크래시
- `respawn()`이 전원을 `state='patrol'`로 되돌림 → 체포 한 번이면 무조건 크래시

수정: `path`가 비어있으면 `homeState`(idle)로 복귀하도록 변경 + `goal` null 가드 추가.
`respawn()`도 원래 자리·각도로 복원하도록 바꿈.

> 이건 비공개 테스트 중인 빌드에서 "가끔 게임이 멈춘다"는 리포트가 있었다면 원인일 가능성이 높음.

---

## 검증 방법

브라우저 없이 최소 DOM 스텁 위에서 스크립트를 실행해 확인함:

- 최상단 초기화(맵/적/랜덤화) 정상 실행
- 4,000프레임 랜덤 입력 시뮬 — 크래시 없음
- 발각 → `detect=100` → `search` → 28초 후 `caution` → 만료 후 `patrol`, 알람 자동 해제 확인
- 처치 → 시체 생성 → 슬로우모션 발동 확인
- 무전 점호 → 전원 경계 전환 확인
- 정지 근무자 강제 `search` → 크래시 없음 (기존 버그 수정 확인)
- 스태미나 소모/회복, 코드 랜덤화 + 쪽지 동기화 확인

**단, 실제 렌더링·터치 입력·오디오는 스텁으로 검증 불가.**
실기기 또는 브라우저에서 아래를 반드시 눈으로 확인할 것:
- 달리기 버튼이 화면에 잘 들어가는지 (버튼이 8개 → 9개로 늘어 세로 공간을 더 차지함)
- 경계도 링이 미니맵·HUD와 겹치지 않는지
- 슬로우모션 중 BGM/효과음이 어색하지 않은지

---

## 남은 작업 (Batch 2 / 3)

### Batch 2 — 콘텐츠 (맵·플로우 수정 필요)
- **#5 비살상 결박** — 기절 후 결박, 미결박 시 일정 시간 뒤 각성
- **#4 시체 운반 페널티** — 현재 은닉이 순간이동이라 리스크가 없음
- **#12 탈출 페이즈** — 미션 3개 완료 시 전체 알람 → 입구까지 탈출
- **#15 장비 선택/픽업** — 시작 시 2종만, 나머지는 맵에서 획득
- **#13 미니게임 2종 추가** — 타이밍 락픽 / 서류 대조
- **#20 튜토리얼 스테이지** — 별도 소형 맵 + 단계별 안내

### Batch 3 — 구조 변경 (설계 결정 필요)
- **#11 스테이지 분리** — 잠입 / 금고 / 탈출 3단계
- **#8 정지 인원 루틴 이동** — 자리 이탈·복귀 경로 필요
- **#18 성능 프로파일링** — `castOmniVisionPolygon`이 적 20명 × 매 프레임. 화면 밖 적 스킵 + fog 버퍼 절반 해상도. **실기기 FPS 측정이 선행되어야 함**

### 우선순위 제안
Batch 2에서 **#20 튜토리얼 → #12 탈출 페이즈 → #5 비살상** 순.
#18은 테스터에게서 실제 FPS 리포트를 받은 뒤 착수하는 게 효율적임.
