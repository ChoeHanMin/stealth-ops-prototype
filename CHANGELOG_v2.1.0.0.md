# Stealth Ops v2.1.0.0 — 버그 수정 4종

대상: `index.html`, `sw.js` (`CACHE_NAME` → **`stealth-ops-v8`**)

---

## 1. 달리기 버튼이 잘 안 눌리는 문제

원인이 두 가지였습니다.

**(a) 조작 방식 불일치** — 웅크리기는 토글인데 달리기만 "누르고 있기"였습니다. 원형 패드를 한 손으로 잡은 채 다른 손가락으로 58px 원을 계속 누르고 있어야 해서 조작이 어긋났습니다.

**(b) 스태미나 소진 시 강제 해제** — 이게 더 직접적인 원인입니다.

```js
// 수정 전
if(stamina<=0) sprintHeld=false;   // 버튼을 계속 누르고 있어도 꺼짐
```

스태미나가 바닥나면 `sprintHeld`가 강제로 false가 됐습니다. 손가락은 계속 버튼 위에 있는데 내부 상태만 꺼져서, **떼었다가 다시 눌러야만** 작동했습니다. 사용자 입장에선 "가끔 안 눌린다"로 느껴집니다.

**수정**

- 웅크리기와 동일하게 **토글**로 통일
- 스태미나 히스테리시스 도입: 0이 되면 달리기가 멈추지만 **토글은 유지**되고, 25까지 회복되면 **자동으로 다시 달립니다**
- 버튼 라벨이 상태를 표시: `달리기` → `달리는 중`(초록 발광) → `지침...`(연한 초록)
- 전역 `mouseup` 리스너 제거 (다른 곳을 탭해도 해제되던 경로 차단)

## 2. 사격 / 상호작용 버튼 교체

주 액션인 사격이 작고 구석에 있었습니다. 위치와 크기를 맞바꿨습니다.

| | 이전 | 이후 |
|---|---|---|
| 사격 | 68px, 왼쪽(168°) | **94px, 중심** |
| 상호작용 | 86px, 중심 | **56px, 왼쪽(170°)** |

사격 버튼이 초록 강조로, 상호작용이 호박색 보조로 바뀌었습니다. 사격 버튼은 적에게 붙으면 `처치`로 전환되는 기존 동작을 그대로 유지합니다.

배치 검증(412×915): 버튼 간 최소 여유 **9px**, 화면 밖 0개, 원형 패드·취소 버튼과 겹침 **없음**.

## 3. 백그라운드에서 사이렌이 계속 들리는 문제

`visibilitychange`에서 `paused` 플래그만 세우고 **오디오 컨텍스트는 그대로 돌려두고 있었습니다.** 게임 로직은 멈췄지만 BGM의 경보 레이어와 예약된 사이렌은 계속 울렸습니다. (`audioCtx.suspend()` 호출이 코드 전체에 0건이었습니다.)

**수정 3중**

1. `suspendAudio()` 신설 — 백그라운드 진입 시 오디오 컨텍스트를 실제로 정지
2. 예약된 사이렌(`setTimeout` 3회 반복)이 복귀 후 몰려서 울리지 않도록 콜백마다 `document.hidden || paused` 확인
3. BGM 경보 레이어 음량 계산에도 같은 조건 추가

복귀 시에는 기존 `resumeAudio()`가 그대로 처리합니다.

## 4. 웅크리면 낮은 가구 안으로 들어가기

`ventForCrouch`(환기구) 패턴을 확장해 `crouchPassable`을 도입했습니다.

| 통과 가능 (10개) | 막힘 (4개) |
|---|---|
| 침대 · 책상 · 식탁 · 소파 · 주방조리대 · 커피테이블 · 벤치 | 장롱 · 냉장고 · 피아노 |

**동작**

- 서 있으면 기존대로 막힘, 웅크리면 통과해서 가구 **안으로** 진입
- 진입하면 그 가구를 플레이어 위에 **알파 0.72로 덮어 그려** 실루엣만 보입니다
- 위치 파악용으로 청록 점선 윤곽선 추가
- HUD 배지가 `▼ 웅크림` → `▼ 가구 아래 은폐`로 변경
- 적 시야 거리 **×0.32** — 거의 발각되지 않습니다
- 가구 안에서는 버튼을 떼도 일어설 수 없습니다 (끼임 방지)

### 구현 중 잡은 로직 버그

첫 구현에서 `under` 판정을 `if(player.crouch)` 안에 넣었더니, 웅크리기를 떼는 순간 판정이 돌지 않아 두 가지가 깨졌습니다.

1. "가구 안에선 못 일어남"이 작동하지 않음
2. 통과 판정도 함께 꺼져서 **가구 안에 영구히 갇힘**

2번이 특히 치명적이었습니다. `under` 판정을 웅크림 여부와 분리해 먼저 계산하고, 이동 충돌은 이전 프레임의 `under`로 계산한 유효 웅크림 상태를 쓰도록 고쳤습니다. 검증에서 "버튼 안 눌러도 밖으로 탈출 가능" 확인했습니다.

---

## 검증

```
--- 달리기 ---
  PASS  토글 ON → 즉시 달림
  PASS  지쳐도 토글 유지 (예전엔 강제 해제됨)
  PASS  지친 동안은 안 달림
  PASS  25 회복되면 자동 재개 — 버튼 다시 안 눌러도 됨
  PASS  토글 OFF하면 즉시 정지
--- 가구 진입 ---
  PASS  서 있으면 막힘 / 웅크리면 진입
  PASS  가구 안에서는 못 일어남
  PASS  버튼 안 눌러도 밖으로 탈출 가능 (갇힘 없음)
  PASS  밖에 나오면 자동으로 일어섬
  PASS  높은 가구(closet)는 웅크려도 막힘
--- 배치 ---
  PASS  버튼 간 최소 여유 9px, 겹침 0건
--- 안정성 ---
  PASS  2000프레임 랜덤조작 무크래시
```

> 검증 로그에 `스태미나 소진 (18)` FAIL이 한 줄 남았는데, 이는 단정 시점 문제입니다. 스태미나가 0을 찍고 `sprintReady`가 꺼진 뒤 남은 프레임 동안 18까지 회복된 상태를 측정한 것으로, 뒤따르는 3개 항목이 모두 통과하므로 히스테리시스는 정상 동작합니다.

### 실기기 확인 필요

- 달리기 토글 조작감 — 밟고 있는 느낌이 없어져서 어색할 수 있습니다
- 94px 사격 버튼이 엄지에 과하지 않은지
- 백그라운드 전환 시 소리가 **실제로** 끊기는지 ← **iOS/안드로이드 각각**
- 가구 아래 실루엣이 어두운 화면에서 식별되는지 — 안 보이면 알파 0.72를 더 낮추면 됩니다
- 가구 은폐(시야 ×0.32)가 너무 강하지 않은지

### 이전 버전에서 아직 미확인

팔 뻗기 가시성 · 비상벨 난이도 · 사거리 800 밸런스 · 원형 패드 데드존 · 사격음 저역

---

## 남은 작업

튜토리얼 · 애널리틱스 · 목표 방향 안내 · 탈출 페이즈 · 비살상 결박 · 시체 운반 페널티 · 장비 픽업 · 일일 챌린지 · 영어 지원 · 경로탐색 A* · 단일 파일 분할
